# ==========================================
# Frontend Environment Configuration
# ==========================================

# API Base URL for backend requests
# Uses reverse proxy — backend is behind the same domain
VITE_API_BASE_URL=https://beta.zeitnahacademy.com/api
